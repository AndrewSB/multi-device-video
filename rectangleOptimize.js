//Andrew

//JS function that will create the largest rectangle of given dimensions (the video) given multiple rectangles (the devices connected)

module.exports = {
  printThis: function (str) {
    console.log(str);
  },
  bar: function () {
    // whatever
  }
};
